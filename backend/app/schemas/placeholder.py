# DELETE THIS FILE once you have created your first real schema module.
# It exists only as a structural placeholder so the schemas/ package is non-empty.
#
# Pattern: one file per feature (e.g. invoice.py, notification.py).
# Define Pydantic BaseModel subclasses here; use *In / *Out / *Update suffixes.
# See notes.py for a worked example.
