# DELETE THIS FILE once you have created your first real repository module.
# It exists only as a structural placeholder so the repositories/ package is non-empty.
#
# Pattern: one file per aggregate (e.g. invoice_repo.py, user_repo.py).
# Accept an AsyncSession as the first argument.
# See notes_repo.py for a worked example.
