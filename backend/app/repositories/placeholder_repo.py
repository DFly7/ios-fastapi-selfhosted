# SCAFFOLDING — not a real module. Safe to delete once you add your first repository.
# It only marks where new repository modules go; it defines nothing and is imported nowhere.
#
# Pattern: one file per aggregate (e.g. invoice_repo.py, user_repo.py),
# accepting an AsyncSession as the first argument.
# Worked example: notes_repo.py
