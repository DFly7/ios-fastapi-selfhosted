# macOS
.DS_Store
**/.DS_Store

# iOS: local DerivedData when using -derivedDataPath (e.g. ios/StarterApp/DerivedDataRun)
**/DerivedDataRun/

# Cursor: optional local MCP config (tokens); team template is .cursor/mcp.json.example
.cursor/mcp.json

# Environment variables
.env

# Per-worktree session simulator UDID (scripts/session-sim.sh / ios-sim.sh)
.sim-udid
